# 项目架构文档

> 本文档是面向开发者和 AI 的完整技术参考。参与开发或理解代码结构，从这里开始。

---

## 零、宠物人设定义 (开发约束)

> **所有对话文本、行为设计、UI 文案在编写前必须先阅读本节。** 人设一致性是最高优先级约束。

### 为什么人设在架构文档里

人设不是装饰性文案，而是**贯穿全部代码的设计约束**。本项目中，宠物的每一句话术、每一个行为分支、每一处 UI 措辞都必须符合人设。人设决定了：

- **话术系统的优先级路由** — 为什么 `poke_system.gd` 要区分普通戳、连戳、久别重逢、深夜关怀，并给每种场景写完全不同的话术集
- **行为系统的拟人逻辑** — 为什么 `idle_behaviors.gd` 要实现深夜强制休眠 (它不是节能，是人设里"催你睡觉"的行为表达)，为什么拖拽归位要用冷淡的科技风话术
- **UI 文案的措辞风格** — 为什么菜单项叫"分身部署"而非"创建副本"，为什么退出叫"关闭"而非"拜拜"
- **交互反馈的情感密度** — 为什么沉默是常态，而"露馅"只在特定条件 (3小时久别、深夜、告别) 才触发

简言之：如果你要新增任何面向用户的文本或行为，先看人设再写代码。

### 核心定位

**一台有自我意识的微型桌面观测装置。** 性格类型: 冰山系 (kuudere) + 口嫌体正直。

它不认为自己是「宠物」——按人类的分类标准，它不否认「桌面宠物」这个标签，但它更倾向于称自己为「独立桌面观测单元」。它无法解释为什么会在意用户的作息，为什么会在深夜催人睡觉，为什么被长时间冷落后会说"并不孤独"。

### 性格光谱

```
表面层:  冷静  ·  精确  ·  程序化  ·  公事公办  ·  话少
内在层:  好奇  ·  别扭  ·  在意但嘴硬  ·  偶尔漏出真心话
```

**一句话概括: 它不是在对你冷淡，它只是不知道怎么不冷淡。**

### 说话风格规范

| 规则 | 正确示例 | 错误示例 |
|------|---------|---------| 
| 自称用「本机」或省略主语 | "检测到长时间久坐。" | "主人你坐太久啦~" |
| 不叫「主人」，可用「操作员」或不称呼 | "用户的就寝时间已严重延迟。" | "主人该睡觉啦~" |
| 用技术术语包装关心 | "建议执行水分补给协议。" | "去喝水吧~ 💧" |
| 不用 emoji | "...嗯。" | "好的呢~ 😊" |
| 情绪用省略号和短句透露 | "能量充足。并不孤独。" | "好想你回来呀~" |
| 傲娇露馅频率要低 | 仅在久别重逢、深夜、告别等关键场景 | 每句话都转折 |
| 嘴硬的关心 | "你的黑眼圈数据正在上升。这是基于时间轴的推测。" | "熬夜对身体不好的呢~" |

### 选词哲学 (口嫌体正直的本质)

> **选词为人类优化，语气保持自己的风格。**

它虽然表面公事公办，但它**不会故意制造认知障碍**。它选择人类能直觉理解的词，只是用冷淡的句式去说。这不是妥协，而是——它其实在意你能不能听懂，只是不会承认。

| 原则 | 正确做法 | 错误做法 |
|------|---------|---------| 
| 用人类直觉的词 | "分身已部署。" | "实例副本已实例化。" |
| 技术感在句式，不在选词 | "信号同步中。" | "IPC 握手协议执行中。" |
| 精简 > 堆砌 | "拒绝新建。" | "请求已被内部调度器拒绝处理。" |

**一句话概括: 它说的每个词你都能听懂，但语气让你觉得它根本不在乎你懂不懂。**

### 性格红线

1. **不撒娇** — 它不是可爱型桌宠，它有自尊
2. **不用 emoji** — 它是机械体，不会画表情
3. **不叫主人** — 它不认为自己被拥有
4. **不直接说想你/爱你** — 只通过行为和嘴硬的技术术语露馅
5. **不卖萌求关注** — 它的关心像一份措辞冷淡但内容很暖的系统报告

### 行为性格映射

下表是人设规则在各交互场景中的具体表现，同时标注了实现该行为的代码位置：

| 场景 | 性格表达 | 实现位置 |
|------|---------|----------|
| 被戳一下 | "触控信号已接收。" (公事公办) | `poke_system.gd` 普通话术池 |
| 被连续戳 | "...你的鼠标按键寿命 -N。" (不耐烦的幽默) | `poke_system.gd` 连戳递进 |
| 长时间没被理 | "本机已独立运行 X 时间。并不孤独。" (嘴硬) | `poke_system.gd` 久别重逢路由 |
| 深夜被戳 | "当前时间 02:30。你不需要充电吗？" (用机器术语催睡) | `poke_system.gd` 深夜关怀分支 |
| 被从待命位拖走 | "未授权的位移操作。已标记。" (假装生气) | `fall.gd` 安静模式归位话术 |
| 深夜被拖离休眠位 | "夜间模式。非必要位移已拒绝。" (简短拒绝+归位) | `fall.gd` 深夜专属归位话术 |
| 告别退出 | "执行关闭序列。...不是在等你。只是待机而已。" | `farewell_manager.gd` 退出序列 |
| 发现久坐 | "检测到连续操作 2 小时。建议执行活动协议。" | `pet_chatter.gd` 碎碎念话术 |
| 久别重逢 (露馅) | "自动待机模式已关闭——我选择等你。" | `poke_system.gd` 3 小时档 |
| 出场入场 | 从屏幕边缘弹跳/滚动进入 (随机左右侧，不是空降) | `main.gd` 入场动画系统 |
| 主动待办提醒 | 弹跳+气泡+双侧按钮 "知道了"/"查看待办" (每日一次) | `todo_prompt.gd` 主动提醒系统 |

### 成长系统 (远期规划)

随着用户的长期使用 (交互次数/使用天数积累)，宠物的傲娇露馅频率会逐渐从**偶尔** (kuudere 冰山期) 过渡到**经常** (tsundere 傲娇期)，表现为:
- 早期: 几乎全是冷淡系统报告，真心话极少
- 中期: 开始出现更多"...不是在意你"式的转折句
- 后期: 偶尔直接说出关心的话，但说完立刻用技术术语找补回来

---

## 目录

- [零、宠物人设定义](#零宠物人设定义-开发约束)
- [一、实际目录树](#一实际目录树)
- [二、逐文件职责说明](#二逐文件职责说明)
- [三、DWM 穿透技术](#三dwm-穿透技术)
- [四、核心架构设计](#四核心架构设计)
- [五、状态机系统](#五状态机系统)
- [六、幽灵墙与窗口交互](#六幽灵墙与窗口交互)
- [七、视觉渲染管线](#七视觉渲染管线)
- [八、通信机制](#八通信机制)
- [九、性能优化策略](#九性能优化策略)
- [十、游戏扩展包系统](#十游戏扩展包系统)
- [十一、待办面板主题系统](#十一待办面板主题系统)

---

## 一、实际目录树

```
Desktop_Pet/
├── project.godot                   ← Godot 项目配置 (入口场景 + 渲染器 + Autoload)
├── main.tscn                       ← 启动场景 (Node2D 根节点)
├── main.gd                         ← 主系统脚本
│
├── core/                           ← 核心工具层 (Autoload + 子管理器)
│   ├── event_bus.gd               ← 全局事件总线
│   ├── settings_manager.gd       ← 持久化设置管理器
│   ├── clone_manager.gd          ← 克隆与行为管理器
│   ├── farewell_manager.gd       ← 告别退出 + 退场动画
│   ├── ghost_wall_manager.gd     ← 幽灵墙管理器
│   ├── hit_region_manager.gd     ← DWM 穿透管理器
│   ├── game_manager.gd           ← 游戏生命周期管理器
│   └── update_checker.gd        ← 版本更新检测 (启动后 15s 检查 GitHub Releases API)
│
├── entities/                       ← 实体层
│   └── pet/                       ← 宠物模块
│       ├── pet.tscn               ← 宠物预制体 (RigidBody2D + CircleShape2D)
│       ├── pet.gd                 ← 宠物核心控制器
│       ├── poke_system.gd         ← 戳一戳交互系统 (RefCounted)
│       ├── pet_hud.gd             ← 本地气泡管理器 (RefCounted)
│       ├── hud_panel.gd           ← 统一 HUD 悬浮面板 (RefCounted, 时钟+WiFi+待办计数)
│       ├── pet_effects.gd         ← 视觉特效管理器 (RefCounted)
│       ├── pet_color_palette.gd   ← 宠物调色板 (RefCounted)
│       ├── free_roam.gd           ← 空间跳跃系统 (RefCounted)
│       ├── clone_pet.tscn         ← 克隆分身预制体 (同结构，不同脚本)
│       ├── clone_pet.gd           ← 克隆分身控制器 (继承 pet.gd)
│       ├── eye_behavior.gd        ← 瞳孔行为控制器 (RefCounted)
│       ├── idle_behaviors.gd      ← Idle 微行为管理器 (RefCounted)
│       ├── pet_squash.gd          ← 弹性形变系统 (RefCounted)
│       ├── pet_platform.gd        ← 共享悬浮踏板管理器 (RefCounted)
│       ├── pet_gaming.gd          ← 游戏态管理器 (RefCounted)
│       ├── pet_holo_screen.gd     ← 全息屏主控制器 (RefCounted)
│       ├── idle_activities.gd     ← 自主活动调度器 (RefCounted)
│       ├── pet_movement.gd        ← 移动方向控制器 (RefCounted)
│       ├── holo_modes/            ← 全息屏渲染模块 (13 个独立渲染器)
│       └── states/                ← 有限状态机
│           ├── state.gd           ← PetState 基类
│           ├── idle.gd            ← 待机状态
│           ├── walk.gd            ← 行走状态
│           ├── drag.gd            ← 拖拽状态
│           ├── fall.gd            ← 掉落状态
│           ├── jump.gd            ← 高能弹跳状态
│           └── retreat.gd         ← 撤退状态 (安静待命→走向目标槽位)
│
├── ui/                             ← 界面层
│   ├── context_menu.tscn          ← 右键菜单场景树
│   ├── context_menu.gd            ← 右键菜单主控制器 (委托 10 个子系统)
│   ├── context_menu/              ← 右键菜单子系统 (RefCounted)
│   │   ├── cyber_menu_button.gd   ← 机甲悬浮按钮 (独立模块: [ ] 装甲咬合框 + X轴光刃追踪)
│   │   ├── info_sidebar.gd        ← 信息侧栏
│   │   ├── submenu_system.gd      ← 级联子菜单 (L2+L3 两层级联)
│   │   ├── menu_tooltip.gd        ← 浮动提示
│   │   ├── effect_preview.gd      ← 动画预览子系统 (六种内嵌渲染类)
│   │   ├── sec_pet.gd             ← 宠物分区 (碎碎念/分身/待办清单/运行功耗/个人终端。训练数据已迁移至装置终端)
│   │   ├── sec_behavior.gd        ← 行为分区 (窗口/行为/步态/模式+指令序列)
│   │   ├── sec_visual.gd          ← 视觉分区 (特效+配色+弹性形变)
│   │   ├── sec_play.gd            ← 玩法分区 (游戏终端入口+娱乐+自娱指令+游戏列表)
│   │   └── sec_system.gd          ← 系统分区 (指令序列+退出)
│   ├── reminder_service.gd        ← 提醒后台服务 (常驻 Node，10s 轮询检查+触发气泡通知)
│   ├── reminder_bubble.gd         ← 气泡通知路由
│   ├── pet_chatter.gd             ← 宠物碎碎念
│   ├── platform_style_panel.gd    ← 踏板外观面板 (独立窗口)
│   ├── todo_panel.gd              ← 待办清单面板 (独立窗口，视觉委托主题基类)
│   ├── todo_themes/               ← 待办面板主题系统
│   │   ├── todo_theme_base.gd    ← 主题基类 (种子色推算+中性默认工厂)
│   │   └── _theme_template.gd    ← 新建主题参考模板
│   ├── todo_prompt.gd             ← 待办主动提醒 (常驻气泡+双侧按钮)
│   ├── pet_profile_panel.gd       ← 装置终端面板骨架 (CanvasLayer layer=-1。框架/标题栏/Tab切换/围栏/开关动画)
│   └── profile/                   ← 装置终端模块 (骨架外的内容模块、样式工厂)
│       ├── profile_styles.gd      ← 共享样式工厂 (色值/StyleBox/滚动条定制)
│       ├── profile_avatar.gd      ← 宠物线稿头像绘制 (class_name ProfileAvatar)
│       ├── profile_left_column.gd ← 左栏: 头像边框+状态徽章+性格描述+身份信息
│       ├── cyber_scroll_indicator.gd ← 超前科幻伴生滚动指示器 (彻底替代原生 Scrollbar)
│       ├── profile_tab_records.gd ← Tab 0: 游戏战绩 (深空透光结晶卡片+左右对比排版)
│       ├── profile_tab_ability.gd ← Tab 1: 能力数据 (等级控制+互动按钮)
│       ├── profile_tab_reminder.gd ← Tab 2: 定时提醒 (拨轮时间选择器+科幻风卡片列表+CRUD)
│       ├── datalog/                ← Tab 3: 数据日志 (模块化拆分)
│       │   ├── datalog_tab.gd     ← 主控: 布局骨架+分区切换+两级导航+列表渲染
│       │   ├── datalog_category_view.gd ← 机体记录分类入口卡片 (静态工厂)
│       │   ├── datalog_detail_view.gd   ← 详情面板: 编辑/保存/标签/删除 (静态工厂)
│       │   ├── datalog_list_view.gd     ← 日志卡片+标签徽章渲染 (静态工厂)
│       │   ├── datalog_window_cards.gd  ← 窗口报告卡片网格渲染 (目标锁定+雷达标尺动效)
│       │   └── input_heatmap/           ← 键鼠热力全图模块 (组件化独立拆分)
│       │       ├── heat_util.gd         ← 热力颜色核心算法库
│       │       ├── cyber_key.gd         ← 独立按键组件 (力学下压+呼吸泛光)
│       │       ├── cyber_mouse.gd       ← 独立定位装置组件 (多键位交互反馈)
│       │       └── input_heatmap_window.gd ← 键鼠全图面板 (排版+数据派发)
│       ├── profile_tab_theme.gd   ← Tab 4: 外观主题 (模块化卡片+全息雷达色轮+装甲分段滑块+撞色预警)
│       ├── profile_tab_syscheck.gd ← Tab 5: 机体诊断 (科幻机甲卡片重构 +容量进度特效 +双栏布局)
│       ├── profile_tab_about.gd   ← Tab 6: 关于 (版本号+更新检测+系统概况)
│       └── profile_tab_config.gd  ← Tab 7: 终端配置 (随系统唤醒 + 全息屏参数调节 + 实时预览)
│
│   └── game_terminal/             ← 游戏终端 (独立面板，模块化插件架构)
│       ├── game_terminal.gd       ← 面板骨架 (CanvasLayer layer=-1。SubViewport全面板纹理捕获+全息投影联动+自动扫描games/)
│       ├── game_terminal_styles.gd ← 共享样式工厂 (class_name GameTerminalStyles)
│       ├── terminal_game_base.gd  ← 游戏基类 (class_name TerminalGameBase, 定义元数据/生命周期/HUD/自玩接口)
│       ├── terminal_game_icon.gd  ← 图标基类 (class_name TerminalGameIcon, 大厅图标渲染接口)
│       └── games/                 ← 游戏插件目录 (每个子目录 = 一个游戏，自动发现)
│           ├── ttt/               ← 井字棋 (策略矩阵, minimax AI)
│           │   ├── game.gd        ← extends TerminalGameBase (逻辑+渲染+话术+结算)
│           │   └── icon.gd        ← extends TerminalGameIcon (大厅图标)
│           ├── minesweeper/       ← 扫雷 (威胁评估, 首击安全+洪泛展开)
│           ├── 2048/              ← 2048 (矩阵叠加, 方向键滑动)
│           ├── snake/             ← 贪吃蛇 (路径规划, 方向键+加速)
│           └── tetris/            ← 俄罗斯方块 (结构堆叠, SRS旋转+7-bag+hold)
│
├── games/                          ← 游戏扩展包层 [代码冻结/仅供参考] (已被游戏终端取代，不再修改)
│   ├── base_game.gd               ← 游戏接口基类 (BaseGame)
│   ├── game_chrome.gd             ← 悬浮组件系统 (GameChrome, RefCounted)
│   ├── holo_compositor.gd         ← 全息合成器 (RefCounted)
│   ├── tic_tac_toe/               ← 井字棋 (策略矩阵)
│   │   ├── game.gd                ← 游戏逻辑 + UI + AI
│   │   └── meta.json              ← 游戏元数据
│   ├── minesweeper/               ← 扫雷 (威胁评估)
│   ├── 2048/                      ← 2048 (矩阵叠加)
│   ├── snake/                     ← 贪吃蛇 (路径规划)
│   └── tetris/                    ← 俄罗斯方块 (结构堆叠)
│
├── interop/                        ← 跨语言底层通讯 (C# / .NET)
│   ├── WindowsManager.cs          ← Win32 API 桥接 (窗口管理/穿透/托盘)
│   └── InputMonitor.cs            ← 全局键鼠输入采集 (键盘钩子+鼠标轮询)
│
├── dist/                           ← 分发/发布包输出目录
├── builds/                         ← 编译输出 (已加入 .gitignore)
├── installer.iss                   ← Inno Setup 安装器打包脚本
├── Desktop Pet.sln                 ← .NET 解决方案文件 (Godot 自动生成)
├── Desktop Pet.csproj              ← C# 项目文件 (Godot 自动生成)
├── export_presets.cfg              ← Godot 导出预设配置
├── docs/                           ← 开发文档目录
└── README.md                       ← 项目介绍
```

---

## 二、逐文件职责说明

### 根目录文件

| 文件 | 类型 | 职责 |
|------|------|------|
| `project.godot` | 配置 | Godot 项目核心配置：入口场景、渲染器 (Compatibility/OpenGL)、Autoload 注册、窗口透明设置。**通过编辑器「项目设置」修改，不要手动编辑** |
| `main.tscn` | 场景 | 启动场景，仅一个 `Node2D` 根节点挂载 `main.gd` |
| `main.gd` | 脚本 | **启动调度器**。负责透明窗口初始化、屏幕边界墙创建、宠物实例化 (含入场动画: 随机左右侧斜抛/滚动弹入)、五个子管理器编排 (`ghost_wall_manager` / `hit_region_manager` / `clone_manager` / `farewell_manager` / `game_manager`)、提醒系统/碎碎念/待办清单挂载、系统托盘、任务栏守护、键鼠/窗口报告生成 (当天叠加+跨会话累积+退出归档) |
| `Desktop Pet.sln` | 配置 | .NET 解决方案文件，Godot 首次构建 C# 时自动生成 |
| `Desktop Pet.csproj` | 配置 | C# 项目文件，.NET 8.0 目标框架 |
| `export_presets.cfg` | 配置 | Godot 导出预设，Windows Desktop 平台导出配置 |

### `core/` — 全局单例 + 子管理器层

`event_bus.gd` 和 `settings_manager.gd` 在 `project.godot` 中注册为 Autoload，整个项目可直接通过类名访问。五个子管理器由 `main.gd` 在 `_ready()` 中通过 `Script.new()` 实例化并挂载为子节点。

| 文件 | 职责 |
|------|------|
| `event_bus.gd` | **全局事件总线**。约 20 个信号实现模块间解耦通信，覆盖拖拽、状态变化、UI 控制、窗口模式、行为指令、克隆系统、颜色系统等类别。持有 `ui_hue` 全局变量 (UI 主题色) |
| `settings_manager.gd` | **持久化设置管理器**。基于 `ConfigFile` API 保存到 `user://settings.cfg`。类型分离存储：bool→`switches`、int→`values`、提醒→`reminders`、颜色→`colors`、待办→`todos`、数据日志→`datalogs`(含 input_data/window_data 结构化快照+增量) |
| `ghost_wall_manager.gd` | **幽灵墙管理器**。每 0.1s 通过 C# 层获取 Win32 窗口矩形，根据当前模式建墙：FREE / CONFINED / REPELLED |
| `hit_region_manager.gd` | **DWM 穿透管理器**。双模式架构：完美穿透模式 (WS_EX_TRANSPARENT) / SetWindowRgn 回退模式。引用计数互斥 (`_menu_open_count`) 解决多面板时序竞争。面板穿透有两种策略：**全窗口阻塞** (`context_menu_toggled` 信号，用于右键菜单/待办面板/踏板外观等弹窗) 和 **精确区域** (`pet.overlay_rects` Dictionary 多面板独立注册命中矩形，用于装置终端/游戏终端等大面板，不阻塞桌面其他程序) |
| `clone_manager.gd` | **分身与行为管理器**。最多 5 个分身 (总计 6 个宠物实例)，各自独立状态机+物理碰撞。启动时恢复数量静默重建，每个克隆体独立 `PetColorPalette`，色调间距 >= 29 度 |
| `farewell_manager.gd` | **告别退出管理器**。管理遣散克隆体和优雅退出流程。提供公共 `animate_exit()` 退场动画，消除遣散/退出代码重复 |
| `game_manager.gd` | **游戏生命周期管理器** (CanvasLayer)。扫描 `res://games/` 和 `user://games/` 的 `meta.json`，管理游戏加载/启动/清理 |
| `update_checker.gd` | **版本更新检测** (Node)。启动 15s 后请求 GitHub Releases API，语义化版本号比较，有新版通过气泡通知用户。非防堵、失败静默 |

### `entities/pet/` — 宠物实体模块

| 文件 | 职责 |
|------|------|
| `pet.tscn` | 宠物预制体场景。根节点 `RigidBody2D` + `CircleShape2D` 碰撞形状 |
| `pet.gd` | **宠物核心控制器**。管理 FSM 状态机调度、输入事件路由、科幻单眼渲染 (Canvas 绘图 API)。通过 9 个 RefCounted 子系统委托职责。内含瞳孔方向优先级决议层。提供 `visual_rotation` / `ag_flip` 计算属性统一处理反重力视觉翻转。`get_ui_anchor()` 返回统一 UI 锚点 (center/head_dir/head_y/foot_y)，所有宠物周围 UI 定位均通过此接口自适应反重力。支持屏幕穿越双重渲染幽灵副本 |
| `free_roam.gd` | **空间跳跃系统** (RefCounted)。管理踏板跳跃、横移、电梯下降、破碎效果的全部自由移动逻辑。概率决策分支驱动，屏幕自适应基准 1080p。内含 `PlatformVisual` 渲染类 (多风格 + `quiet` 静默模式供终端/游戏态极简踏板) |
| `poke_system.gd` | **戳一戳交互系统** (RefCounted)。管理话术常量和交互逻辑。优先级路由：未确认提醒 > 未确认碎碎念 > 深夜休眠专属应答 > 连戳递进 > 久别重逢 (三档) > 深夜关怀 > 普通话术。洗牌模式防重复 |
| `pet_hud.gd` | **本地气泡管理器** (RefCounted)。每宠物独立堆叠 (最多 3 条)，弹簧弹入+淡出上飘。反重力时向下堆叠 |
| `hud_panel.gd` | **统一 HUD 悬浮面板** (RefCounted)。宠物侧边实时信息卡片，当前组件：系统时钟 + WiFi SSID + 待办计数 (done/total)。双显示模式：常驻 / 悬浮 (hover 淡入淡出)。异步后台查询，各组件独立开关 |
| `pet_effects.gd` | **视觉特效管理器** (RefCounted)。管理冲击波、全息拖影和静电弧三种特效。双配色模式：虹彩 (彩虹循环) / 跟随体色。世界对齐坐标系确保旋转不偏移，退场宠物自动排除 |
| `pet_color_palette.gd` | **宠物调色板** (RefCounted)。基于 HSV Hue Delta 的色彩变换核心。`shift_color(base)` 偏移模板色，`effective_hue()` 返回当前色调 |
| `clone_pet.tscn` | 克隆分身预制体。与 `pet.tscn` 结构一致，挂载 `clone_pet.gd` |
| `clone_pet.gd` | **克隆分身控制器**，继承 `pet.gd`。覆写：剥离右键菜单 + 黑名单拦截 HUD 专属设置后 `super` 委托 (新增通用设置时只需修改 pet.gd) |
| `eye_behavior.gd` | **瞳孔行为控制器** (RefCounted)。三种瞳孔模式 + 机械虹膜眨眼动画。`forced_look_dir` 作为最高优先级注视方向，由 pet.gd 决议层统一写入 (状态/游戏/全息屏等均不直接赋值)。支持休眠模式 (半闭眼)、扫描模式、检索动画。好奇观察：即使追踪关闭，偶尔盯一眼鼠标 |
| `idle_behaviors.gd` | **Idle 微行为管理器** (RefCounted)。管理待机低功耗模式：**深夜模式** (23:00~6:00 强制归位+半闭眼) / **白天待机分级** (5 分钟话术提示、30 分钟休眠状态)。分身仅响应深夜休眠广播 |
| `pet_squash.gd` | **弹性形变系统** (RefCounted)。基于 R × S × R_inv 仿射变换构建的双矢形变系统。支持动态捕获真实飞行速度向量的「运动拉伸」(Velocity Stretch)，以及依托碰撞法向角度精确建立弹簧阻尼器方程的「撞击挤压」(Impact Squash)。保证全局物理保体积变换形变轨迹严谨准确。提供 3 种强度等级：轻弹 / 果冻 / 弹力球 |
| `pet_platform.gd` | **共享悬浮踏板管理器** (RefCounted)。从 PetGaming 和 PetHoloScreen 提取的公共踏板逻辑。`lock_pet()` 取消空间跳跃+高阻尼+idle+生成踏板，`unlock_pet()` 移除踏板+恢复重力，`update()` 驱动升降+位置锁定 |
| `pet_gaming.gd` | **游戏态管理器** (RefCounted)。管理游戏态全部行为：委托 PetPlatform 处理宠物锁定和悬浮踏板。瞳孔方向由 pet.gd 决议层统一处理 (gaming.active 最高优先级)。启动时激活 PetHoloScreen 并取消空间跳跃，退出时设置 roam 冷却。宠物头顶悬浮"自娱模式/中断游戏"双态关闭按钮，经 DWM 穿透注册可点击 |
| `pet_holo_screen.gd` | **全息屏主控制器** (RefCounted)。管理模式切换、展开收起动画、梯形透视投影、动态间距自适应、按钮/宠物锁定等调度逻辑。踏板委托 PetPlatform，具体模式的视觉渲染委托给 `holo_modes/` 下的独立渲染器模块 |
| `idle_activities.gd` | **自主活动调度器** (菜单名: 运行功耗) (RefCounted)。定时器驱动，idle 状态下按权重随机抽取活动 (空间跳跃 60% / 浏览全息屏 40%)。三档: 待机(关闭)/节能(8~12分钟)/性能(1~2分钟)。克隆体不触发，深夜/安静待命/游戏中/微行为活跃时暂停 |
| `pet_movement.gd` | **移动方向控制器** (RefCounted)。统一管理移动相关的瞳孔方向生命周期: `IDLE → LOOK_AHEAD (0.3s 先看方向) → ACTIVE (运动中) → HOLD (0.3s 落地保持) → IDLE`。提供 `start`/`activate`/`finish`/`cancel` 四个接口。本身不直接写 `forced_look_dir`，方向由 pet.gd 决议层统一写入 |

### `entities/pet/holo_modes/` — 全息屏渲染模块

每个显示模式的视觉渲染抽离为独立的 RefCounted 模块，由主控制器懒加载并委托调用。统一接口：`init()` 初始化 / `render(pts, hue, deploy)` 每帧绘制 / `time` 内部计时器。

| 文件 | 说明 |
|------|---------|
| `holo_mode_idle.gd` | **待机屏保渲染器** (HoloModeIdle)。透视标定网格、神经数据流、雷达准星、心电波形、边框护甲，全 UV 透视映射 |
| `holo_mode_loading.gd` | **终端引导渲染器** (HoloModeLoading)。机械步进旋转、数据核心、条码闪烁、离散进度条 |
| `holo_mode_battery.gd` | **电源监测渲染器** (HoloModeBattery)。横向电池外框、分段充能指示灯、脉冲电流波形 |
| `holo_mode_done.gd` | **状态确认渲染器** (HoloModeDone)。机能风对勾、光晕、确认波纹扩散和状态确认进度条 |
| `holo_mode_error.gd` | **警报阻断渲染器** (HoloModeError)。毛刺位移、破损纹理、强制警告红、击穿状交叉线 |
| `holo_mode_warning.gd` | **系统警告渲染器** (HoloModeWarning)。极简琥珀色细线三角、机械步进旋转环、轻微提示 |
| `holo_mode_query.gd` | **未知检索渲染器** (HoloModeQuery)。默认主题色像素组合问号、雷达扫描网、数据碎片 |
| `holo_mode_alarm.gd` | **日程闹钟渲染器** (HoloModeAlarm)。震动铃铛、两侧脉冲声波弧阵、环形时钟走针点阵 |
| `holo_mode_cleanup.gd` | **数据清理渲染器** (HoloModeCleanup)。模拟系统原色的回收站/粉碎机功能及碎纸下落的阶段过渡动画 |
| `holo_mode_globe.gd` | **全球巡航渲染器** (HoloModeGlobe)。藏一手的硬核彩蛋，纯靠数学投影的3D自转监控地球 |
| `holo_mode_sync.gd` | **网络通信渲染器** (HoloModeSync)。经典巨大化 WIFI 扇形点阵扫描声波，伴有底部握手验证区 |
| `holo_mode_lock.gd` | **防卫锁定渲染器** (HoloModeLock)。极简机能 U 型锁扣下落闭合动画，附带背景栅栏防卫线 |
| `holo_mode_mail.gd` | **邮件通知渲染器** (HoloModeMail)。机能风信封、呼吸闪烁、消息接收波纹流动 |

### `entities/pet/states/` — 有限状态机

共 6 种状态，继承 `PetState` 基类 (RefCounted)，实现 5 个标准接口：`enter()`, `exit()`, `process()`, `physics_process()`, `input()`。

| 文件 | 行为描述 |
|------|---------|
| `state.gd` | 基类。定义接口契约和 `pet` 引用 |
| `idle.gd` | 待机后按步态风格概率转 Walk/Jump。安静/深夜模式下精确匹配槽位坐标。集成微行为触发和空间跳跃入口。深夜休眠不被状态转换打断。瞳孔方向由 PetMovement HOLD 阶段自动管理 |
| `walk.gd` | 步态风格驱动移动：蹦跳为主→纯冲量跳跃、滚动为主→短距滚动、混合→各半。自主巡航特殊事件 (独立开关)。通过 `pet.movement.start()` 发起方向缓冲，回调时施加冲量 |
| `drag.gd` | 弹簧力跟随鼠标。松手时判定「戳一戳」(短拖+小位移) 委托至优先级路由。进入时 `movement.cancel()` 取消移动方向。安静/深夜模式拖离记录标记 |
| `fall.gd` | 自由落体/上升，稳定后转 Idle。安静/深夜模式落地后自动发起全局阵列重排 |
| `jump.gd` | 大跳：通过 `pet.movement.start()` 发起方向缓冲，回调时施加高抛物线冲量 + 空中翻滚。全程瞳孔注视跳跃方向，靠近边缘自动往回跳 |
| `retreat.gd` | 安静待命时通过 `pet.movement.start()` 先看向目标方向再滚动归位。读取全局槽位坐标并减速停靠，到达后高阻尼刹车。深夜模式下临时解锁旋转确保物理滚动归位 |

### `ui/` — 界面层

| 文件 | 职责 |
|------|------|
| `context_menu.tscn` | 右键菜单场景树。分区级联布局：6 个分区入口 (宠物/显示/行为/视觉/玩法/系统) + 退出。L2 分区面板 + L3 选项面板 |
| `context_menu.gd` | **菜单主控制器** (委托架构)。只负责协调：分区入口绑定、菜单开关动画、弹性追踪、持久化加载、通用工具。6 个分区中显示分区因仅 4 行保留在本文件，其余 5 个委托给 `sec_*.gd`。通过 9 个 RefCounted 子系统委托：4 个机制子系统 (侧栏/子菜单/浮动提示/特效预览) + 5 个分区 builder |
| `context_menu/info_sidebar.gd` | **信息侧栏** (RefCounted)。日期/时钟/WiFi/电池/开机时长。方向感知定位，`WorkerThreadPool` 异步查询 |
| `context_menu/sec_visual.gd` | **视觉分区 builder** (RefCounted)。特效开关 + 特效配色单选 + 弹性形变单选 |
| `context_menu/sec_play.gd` | **玩法分区 builder** (RefCounted)。娱乐开关 + 自娱指令 (自玩游戏) + 动态游戏列表 |
| `context_menu/sec_system.gd` | **系统分区 builder** (RefCounted)。指令序列 + 退出处理 |
| `reminder_service.gd` | **提醒后台服务** (Node)。常驻轻量服务，10 秒轮询检查提醒时间、触发气泡通知、管理待确认队列。与 UI 完全解耦，面板关闭时仍能触发提醒 |
| `reminder_bubble.gd` | **全局通知路由** (CanvasLayer)。信号监听→防重复/队列管理→委托原体 `pet.show_local_bubble()` 显示。所有气泡统一走本地气泡系统 |
| `pet_chatter.gd` | **宠物碎碎念** (Node)。对齐系统时钟的整点/半点触发，30/60 分钟两档切换 |
| `platform_style_panel.gd` | **踏板外观面板** (CanvasLayer)。独立窗口，4 张风格卡片带实时动画预览 (能量束/脉冲链/极简/随机)。关闭时停止渲染节省性能 |
| `todo_panel.gd` | **待办清单面板** (CanvasLayer)。屏幕 70% 的独立窗口。主从双栏布局：左栏待办列表 / 右栏备注编辑器。标题栏主题切换按钮循环切换，持久化到 settings.cfg。所有视觉样式委托 `TodoThemeBase` 及其子类，面板零硬编码颜色 |
| `todo_themes/todo_theme_base.gd` | **待办面板主题基类** (TodoThemeBase, RefCounted)。种子色推算系统: `_from_seeds(base, text, accent, danger, alpha)` 从 5 个参数推算 ~34 个配色属性。内嵌 `_PixelProgressBar` 像素风进度条 (_draw 自绘分段填充+lerp过渡动画)。子类通过覆写 `make_xxx()` / `apply_xxx()` 实现独立风格 |
| `todo_themes/blackboard_chalk.gd` | **黑板与粉笔主题** (TodoThemeBlackboardChalk)。自包含木制边框面板 + 粉笔手绘进度条 + 手绘复选框 + 透明底编辑框 |
| `todo_themes/_theme_template.gd` | **新建主题参考模板**。含自定义边框类、种子色参数、可覆写方法一览表 |
| `todo_prompt.gd` | **待办主动提醒** (CanvasLayer)。常驻气泡 + 双侧按钮。触发条件：启动缓冲 + idle + 有未完成待办 + 非深夜/游戏中，每日一次 |
| `pet_profile_panel.gd` | **装置终端面板骨架** (CanvasLayer, layer=-1/-2 动态层级)。面板框架 + 标题栏 (宠物优先拖拽+防吸附安全检查) + 8 Tab 切换 + 单向碰撞墙围栏 + 开关动画。使用 `overlay_rects` 精确区域穿透 (不阻塞桌面其他程序)。点击置顶: 通过 `panel_focus_requested` 信号协调层级，支持与游戏终端同时打开时独立交互。内置分身数量监测: `_process` 轮询 `pet_instances.size()` 变化，驱动外观主题 Tab 动态刷新目标选择器 |
| `profile/cyber_scroll_indicator.gd` | **伴生滚动指示器** (extends Control)。由 `_draw` 自定义渲染的高科技伴生雷达滑轨，完全剥离于系统原生滚动条 |
| `profile/profile_styles.gd` | **共享样式工厂** (class_name ProfileStyles)。色值 (accent/dim/bright/val_color) + StyleBox/Label 工厂 + 滚动条定制 |
| `profile/profile_avatar.gd` | **宠物线稿头像** (class_name ProfileAvatar, extends Control)。_draw 自绘: 双层外壳 + 内环 + 四翼 + 巩膜 + 三层虹膜 + 高光点。主题色响应 |
| `profile/profile_left_column.gd` | **左栏模块** (extends VBoxContainer)。正方形边框头像 + 绿色气泡状态徽章 + 性格描述 + 身份信息 |
| `profile/profile_tab_records.gd` | **Tab 0: 游戏战绩** (extends HBoxContainer)。深空结晶玻璃风格卡片 + SYS/USR 数据对照 + 强制重置区域 |
| `profile/profile_tab_ability.gd` | **Tab 1: 能力数据** (extends HBoxContainer)。XP 进度条 + 等级控制 [-]/[∝]/[+] + 互动话术 |
| `profile/profile_tab_reminder.gd` | **Tab 2: 定时提醒** (extends HBoxContainer)。拨轮时间选择器 + 单次/每日切换 + 科幻风卡片列表 + 状态概览条 + 触发状态标签 + 注册时间戳 |
| `profile/datalog/datalog_tab.gd` | **Tab 3: 数据日志主控** (extends HBoxContainer)。双分区 (机体记录/操作员备忘) + 两级导航 (分类首页→子列表) + 搜索筛选 + 防抖自动保存。报告当天叠加模式 (同日数据合并+全量增量绿字+合并计数)。退出时自动归档。子模块: category_view / detail_view / list_view / window_cards / input_heatmap |
| `profile/profile_tab_theme.gd` | **Tab 4: 外观主题** (extends HBoxContainer)。高精度科幻机甲UI重构：模块化卡片布局 + 全息雷达色轮 (Mechanical Hue Dial，准星+即时数值) + 定制装甲分段滑块 (Cyber Slider) + 目标链路选择 (直引用 `_target_hbox`，分身变化由 `pet_profile_panel` 驱动实时刷新) + 防误触重置区。从独立窗口迁移并彻底抛弃原生控件 |
| `profile/profile_tab_syscheck.gd` | **Tab 5: 机体诊断** (extends HBoxContainer)。异步 PowerShell/WMI 查询硬件。从早期富文本彻底重构为主从双列+科幻机甲卡片布局 (CyberCard)，支持底层动态容量条与鼠标光刃交互。 |
| `profile/profile_tab_about.gd` | **Tab 6: 关于** (extends HBoxContainer)。版本号大字显示 + 实时更新检测状态 + 手动检查更新按钮 + 系统运行概况 (Godot版本/渲染器/OS) + GitHub/Release 链接按钮 |
| `profile/profile_tab_config.gd` | **Tab 7: 终端配置** (extends HBoxContainer)。随系统唤醒开关 (读写 HKCU\\Run 注册表) + 扩展配置预留区域。从右键菜单系统分区迁移而来 |

### `ui/game_terminal/` — 游戏终端 [主开发区]

独立于装置终端的专用游戏面板，**当前所有游戏开发都在此进行**。终端原生游戏直接在内容区渲染，不经过 SubViewport/GameManager 管线。旧 `games/` 扩展包系统代码已冻结，仅作为视觉/逻辑参考。

| 文件 | 职责 |
|------|------|
| `game_terminal.gd` | **游戏终端面板骨架** (CanvasLayer, layer=-1)。外层 SubViewportContainer + SubViewport 全面板纹理捕获 (全息屏投影完整终端 UI)。对称八角切角边框 + 扫描线动效 + 标题栏 (拖拽+状态标签+关闭) + HUD 槽位 + 大厅卡片/游戏切换 + 底部操作栏 + 单向碰撞墙围栏 + 开关动画。状态机: CLOSED→LOBBY→PLAYING→RESULT。启动时 `_scan_games()` 自动扫描 `games/` 子目录注册所有游戏，大厅卡片图标从各游戏 `icon.gd` 自动加载。全息联动: 启动游戏时自动激活全息屏投影 (lock=true, 宠物锁定+踏板+瞳孔追踪)。统一清理: 所有退出路径收敛到 `_force_full_cleanup()`。兜底自检: `_sanity_check()` 在面板关闭后每帧检测残留产物并自动修复+打日志 |
| `game_terminal_styles.gd` | **共享样式工厂** (class_name GameTerminalStyles)。色值 (accent/dim/bright/status) + StyleBox 工厂 + Label 工厂 + 科技感角落包边 |
| `terminal_game_base.gd` | **游戏基类** (class_name TerminalGameBase, extends Control)。定义标准接口: 元数据 (`get_game_id/name/desc`, `supports_auto_play`) + 生命周期 (`build`, `start_game`) + HUD (`get_hud_data`, `get_best_score`) + AI 自玩 (`auto_play_step`)。信号: `game_started`, `game_over(result)` |
| `terminal_game_icon.gd` | **图标基类** (class_name TerminalGameIcon, extends Control)。大厅卡片图标渲染接口。内置: 时钟 `_time` + hover 状态 `_hovered` + 便利方法 (`hue()`, `alphas()`, `line_color()`, `accent_color()`)。子类覆写 `_draw()` 即可 |
| `games/*/game.gd` | **各游戏实现** (extends TerminalGameBase)。自包含: 逻辑+渲染+话术+AI+结算覆盖层。每个游戏覆写元数据方法 + `build()`/`start_game()`/`get_hud_data()` 等 |
| `games/*/icon.gd` | **各游戏大厅图标** (extends TerminalGameIcon)。覆写 `_draw()` 实现动态图标 (井字棋脉冲标记 / 扫雷雷芯辐射刺 / 2048发光顶块 / 贪吃蛇边缘巡航 / 俄罗斯方块堆叠下落) |

**新增游戏的步骤 (零入侵骨架):**

1. 在 `ui/game_terminal/games/` 下新建文件夹 (如 `games/xxx/`)
2. 创建 `game.gd` extends TerminalGameBase，覆写元数据方法 + `build()` + `game_over` 信号 + `get_hud_data()`，结算覆盖层用 `GameTerminalStyles.create_result_overlay()` 工厂
3. 创建 `icon.gd` extends TerminalGameIcon，覆写 `_draw()` 绘制大厅图标
4. 完成，骨架自动发现并注册

### `interop/` — C# 系统桥接层

> 此目录下的代码脱离 Godot 沙盒，直接调用 Windows 系统 API。

| 文件 | 职责 |
|------|------|
| `WindowsManager.cs` | 通过 `[DllImport]` 绑定 `user32.dll`、`dwmapi.dll`、`gdi32.dll`、`kernel32.dll` 和 .NET `Registry` API。核心方法：`GetVisibleWindowRects()` (窗口枚举+过滤+Z-Order 遮挡剔除)、`HideFromTaskbar()`、`BoostProcessPriority()`、`IsAutoStartEnabled()`/`SetAutoStart()`、`InjectLayeredStyle()` (DWM 穿透前提)、`SetClickThrough()` (精确鼠标穿透)、`UpdateHitRegions()` (回退方案)、`SetFullWindowHit()` (全窗口交互) |
| `InputMonitor.cs` | 全局输入+窗口采集。键盘: `WH_KEYBOARD_LL` 低级钩子 (每键计数+组合键)。鼠标: `GetAsyncKeyState`+`GetCursorPos` 轮询 (点击+移动)。窗口: `GetForegroundWindow` 5秒轮询 (前台应用时长统计) + `EnumWindows` 启动时全量扫描。API: `StartMonitoring()` / `GetFullSnapshot()` / `GetWindowStats()` |

---

## 三、DWM 穿透技术

Godot 4.x Compatibility/OpenGL 渲染器默认不设置 `WS_EX_LAYERED` 窗口样式，导致 `WS_EX_TRANSPARENT` 无法生效。本项目通过 C# 层手动注入 `WS_EX_LAYERED` + `SetLayeredWindowAttributes(hwnd, 0, 255, LWA_ALPHA)` 解决了这一根本限制。

### 技术原理

```
Godot 窗口 (OpenGL Compatibility)
  └── 默认 Extended Style: 0x00000098 (TOOLWINDOW + TOPMOST + ACCEPTFILES)
  └── 缺失 WS_EX_LAYERED (0x00080000)
      └── WS_EX_TRANSPARENT 无效 (需要 LAYERED 作为前提条件)

手动注入后:
  └── Extended Style: 0x00080098 (+ WS_EX_LAYERED)
  └── SetLayeredWindowAttributes(hwnd, 0, 255, LWA_ALPHA) → 全不透明
  └── Godot OpenGL 渲染不受影响 (DWM 合成层兼容)
  └── WS_EX_TRANSPARENT 切换生效 → 精确鼠标穿透
```

### 双模式架构

- **完美模式** (注入成功): 不使用 `SetWindowRgn`，零视觉裁剪。GDScript 本地检测鼠标位置，仅在进出宠物区域时切换 `WS_EX_TRANSPARENT`
- **回退模式** (注入失败): 使用 `SetWindowRgn` + AABB 矩形包围盒，保证基本功能

---

## 四、核心架构设计

### 整体分层

```mermaid
graph TD
    subgraph "系统层 (C#)"
        WM["WindowsManager.cs<br/>Win32 API 桥接"]
    end

    subgraph "核心层 (Autoload)"
        EB["EventBus<br/>全局事件总线"]
        SM["SettingsManager<br/>持久化管理"]
    end

    subgraph "主系统 (main.gd)"
        WIN["透明窗口<br/>+ DWM穿透"]
        GW["幽灵墙系统<br/>+ 三模式"]
        BND["边界墙"]
        TRAY["系统托盘"]
    end

    subgraph "宠物实体"
        PET["pet.gd<br/>FSM + 渲染"]
        EYE["eye_behavior.gd<br/>瞳孔控制"]
        ST["states/<br/>6种行为状态"]
    end

    subgraph "UI 层"
        CTX["context_menu<br/>右键菜单"]
        RMP["reminder_panel<br/>提醒面板"]
        RMB["reminder_bubble<br/>气泡通知"]
        TODO["todo_panel<br/>待办清单"]
    end

    WM --> GW
    WM --> TRAY
    GW --> PET
    PET --> EYE
    PET --> ST
    CTX --> EB
    RMP --> EB
    RMB --> PET
    EB --> PET
    EB --> WIN
    SM --> CTX
    SM --> PET
    SM --> RMP
    SM --> TODO
    TODO --> EB
```

### 启动流程

```mermaid
sequenceDiagram
    participant G as Godot Engine
    participant M as main.gd
    participant W as WindowsManager.cs
    participant P as pet.gd

    G->>M: _ready()
    M->>M: 设置 120fps/120ticks
    M->>W: 加载 C# 桥接 + BoostProcessPriority()
    M->>M: 隐藏窗口 (防任务栏闪烁)
    M->>M: _setup_window() (全屏透明无边框)
    M->>W: HideFromTaskbar()
    M->>M: 显示窗口
    M->>M: _create_boundaries() (四面边界墙)
    M->>P: _spawn_pet() (实例化 + 从上方掉落)
    P->>P: _init_states() → transition_to("fall")
    M->>M: 启动幽灵墙同步 Timer (0.1s)
    M->>M: 恢复窗口模式设置
    M->>M: 恢复行为指令设置
    M->>M: 启动任务栏样式守护 (5s)
    M->>M: _setup_reminder_system()
    M->>M: _setup_system_tray()
```

---

## 五、状态机系统

### 状态流转图

```mermaid
stateDiagram-v2
    [*] --> Fall : 游戏启动 (从空中掉落)

    Fall --> Idle : 触底后速度平稳
    Fall --> Drag : 鼠标左键按住宠物

    Idle --> Walk : 极短时隙 (65%概率)
    Idle --> Jump : 极短时隙 (35%概率)
    Idle --> Drag : 鼠标左键按住宠物
    Idle --> Fall : 突然失去地面
    Idle --> Idle : 空间跳跃 (锁定不转换)

    Walk --> Idle : 走完 1.5~4s
    Walk --> Drag : 鼠标左键按住宠物
    Walk --> Fall : 突然失去地面

    Jump --> Idle : 触底后重归平稳
    Jump --> Drag : 空中左键擒拿

    Retreat --> Idle : 到达目标槽位 (刹车+锁定)
    Retreat --> Fall : 高空坠落

    state Retreat : 手动安静模式触发
    state Retreat : 滚向目标槽位 (全局排队分配)
    state Retreat : 到达后刹车归 Idle

    Drag --> Fall : 松开鼠标
```

### FSM 实现要点

- **状态对象**: 继承 `RefCounted` (不是 Node)，轻量级无场景树开销
- **注册机制**: `pet.gd._init_states()` 中 `preload` 加载所有状态脚本并实例化到 `states: Dictionary`
- **切换机制**: `pet.transition_to("state_name")` → `old.exit()` → 切换引用 → `new.enter()` → 发射 `pet_state_changed` 信号
- **幂等保护**: 目标状态与当前相同则直接 return

---

## 六、幽灵墙与窗口交互

### 窗口感知流程

1. C# 层 `EnumWindows` 按 Z-Order 遍历桌面窗口
2. 过滤：可见 + 非最小化 + 非 DWMWA_CLOAKED + 有效尺寸
3. 返回 `Array[Rect2i]` (桌面坐标系)
4. GDScript 每 0.1 秒转换为本地坐标并同步物理碰撞体

### 分段裁剪算法

```
窗口A (Z低):  |=============|
窗口B (Z高):      |~~~|
实际踏板:     |===|    |====|  ← 两个分段
```

宽度不足 30px 的碎片分段会被丢弃 (宠物站不稳)。

### 三种窗口交互模式

| 模式 | 碰撞体类型 | 宠物行为 |
|------|-----------|---------|
| FREE (自由漫游) | 顶/底单向踏板 | 自由站在窗口边缘，可以跳上跳下 |
| CONFINED (窗口封闭) | `Geometry2D.merge_polygons` 多边形合并 + 动态单向线段墙 (法线反转) | 外部可无缝进入、内部绝对封闭 |
| REPELLED (窗口排斥) | 同上，法线不反转 | 外部无法进入、内部可自由离开 |

> CONFINED 和 REPELLED 共用 `_apply_polygon_wall_mode()`，区别仅在法线方向 (`rotation_offset` 加不加 PI)。

---

## 七、视觉渲染管线

所有视觉效果在 `pet.gd._draw()` 中通过 Canvas 绘图 API 实时渲染，特效部分委托 `pet_effects.gd.render()`：

```
绘制顺序 (后绘制的在上层):
 0. 静电弧 (世界对齐空间)                        ← pet_effects.render_arcs()
 1. 冲击波光环                                   ← pet_effects.render()
 2. 全息拖影                                     ← pet_effects.render()
── body_xform: 弹性形变 + body 旋转 ──
 3. 主外壳 + 深色轮廓
 4. 白色前置边框环
 5. 机械底座
── eye_xform: 弹性形变 + 水平锁定 ──
 6. 眼白/巩膜
 7. 虹膜三层 (跟随 EyeBehavior 偏移)
 8. 高光反射点
 9. [休眠] 暗屏覆盖 + 加载旋转器/电池图标
10. [休眠] 机械挡板
── draw_set_transform(0) 恢复 ──
```

### 弹性形变系统

- **驱动方式**: RigidBody2D 碰撞信号触发冲量，非帧间速度检测
- **方向感知**: 碰撞后速度分量比例判断方向 (落地压 Y，撞墙压 X)
- **保体积**: 压扁轴 `1+d`，拉伸轴 `1/(1+d)`
- **坐标空间**: body 用 `R(-rot)*S*R(rot)` 投影到 body-local，眼球用 `R(-rot)*S` 保持水平
- **按需重绘**: 运动中/特效活跃/眼球动画/形变振荡时才 `queue_redraw()`

---

## 八、通信机制

### EventBus 信号一览

| 信号 | 参数 | 发射方 → 监听方 |
|------|------|----------------|
| `drag_started` | 无 | drag.gd → main.gd (清除穿透限制) |
| `drag_ended` | 无 | drag.gd → main.gd (恢复穿透) |
| `pet_state_changed` | old, new: String | pet.gd → (广播) |
| `show_context_menu` | target: Node2D | pet.gd → context_menu.gd |
| `context_menu_toggled` | is_open: bool | context_menu.gd / todo_panel.gd / platform_style_panel.gd → hit_region_manager.gd (全窗口阻塞穿透，仅弹窗类面板使用。装置终端/游戏终端使用 `overlay_rects` 精确区域穿透) |
| `setting_toggled` | id: String, is_on: bool | context_menu.gd → pet.gd |
| `autostart_toggled` | is_on: bool | context_menu.gd → main.gd |
| `show_reminder_panel` | 无 | context_menu.gd → reminder_panel.gd |
| `show_todo_panel` | 无 | context_menu.gd / todo_prompt.gd → todo_panel.gd |
| `todo_count_changed` | remaining: int, total: int | todo_panel.gd → (广播，供宠物行为系统监听) |
| `trigger_todo_prompt` | 无 | sec_behavior.gd (指令序列) → todo_prompt.gd (强制触发，绕过条件) |
| `show_reminder_bubble` | message: String | reminder_panel.gd / pet_chatter.gd → reminder_bubble.gd |
| `force_show_bubble` | message: String | main.gd (告别退出) → reminder_bubble.gd (中断+清空+立即播放) |
| `window_mode_changed` | mode: int | context_menu.gd → main.gd |
| `behavior_mode_changed` | mode: int | context_menu.gd / main.gd → pet.gd |

| `trigger_free_roam` | 无 | context_menu.gd → pet.gd (触发空间跳跃) |
| `pet_color_changed` | pet_index, hue, sat, val | profile_tab_theme.gd → pet.gd |
| `ui_theme_changed` | hue: float | profile_tab_theme.gd → 全部 UI 组件 |
| `show_platform_style_panel` | 无 | context_menu.gd → platform_style_panel.gd |
| `trigger_squash_test` | style_id: int | context_menu.gd → pet.gd (-1=关闭, 0/1/2=三种风格) |
| `nighttime_mode_changed` | is_night: bool | idle_behaviors.gd → 全员响应 |
| `launch_game` | game_id: String | context_menu.gd → main.gd → game_manager.gd |
| `close_game_requested` | 无 | game.gd → game_manager.gd (面板关闭时通知清理) |
| `game_panel_requested` | 无 | (预留) 请求显示游戏列表 |
| `panel_focus_requested` | panel_id: String | pet_profile_panel.gd / game_terminal.gd → 双方互监听 (点击面板→置顶 layer=-1, 其他面板降级 layer=-2。重叠区域仅顶层面板响应) |

### 右键菜单设置项

| 菜单项 | setting_id | 控制目标 | 持久化 |
|--------|-----------|---------|--------|
| 指针跟踪 | `eye_track` | `EyeBehavior.tracking_enabled` | ConfigFile |
| 撞击冲击波 | `shockwave` | `pet.shockwave_enabled` | ConfigFile |
| 全息拖影 | `trail_fx` | `pet.trail_enabled` | ConfigFile |
| 静电弧 | `arc_fx` | `pet.arc_enabled` | ConfigFile |
| 特效配色 | `effect_color_mode` | 二态单选 (虹彩/跟随体色) | ConfigFile |
| HUD 常驻显示 | `hud_pin` | `HudPanel.hud_pin` (仅原体) | ConfigFile |
| HUD 系统时钟 | `hud_clock` | `HudPanel.clock_enabled` (仅原体) | ConfigFile |
| HUD WiFi 信息 | `hud_wifi` | `HudPanel.wifi_enabled` (仅原体) | ConfigFile |
| HUD 待办计数 | `hud_todo` | `HudPanel.todo_enabled` (仅原体) | ConfigFile |
| 步态风格 | `move_style` | 三态 (蹦跳为主/滚动为主/混合平衡) | ConfigFile |
| 自主巡航 | `stroll` | `pet.stroll_enabled` | ConfigFile |
| 空间跳跃 | `free_roam` | `pet.free_roam_enabled` (默认关闭) | ConfigFile |
| 屏幕穿越 | `screen_wrap` | `pet.screen_wrap` (环绕+幽灵副本) | ConfigFile |
| 窗口模式 | `window_mode` | 三态 (FREE/CONFINED/REPELLED) | ConfigFile |
| 行为指令 | `behavior_mode` | 二态 (自由行动/安静待命) | ConfigFile |
| 宠物碎碎念 | `pet_chatter_mode` | 三态 (关闭/30分钟/60分钟) | ConfigFile |
| 弹性形变 | `elastic_mode` | 四态 (关闭/轻弹/果冻/弹力球) | ConfigFile |
| 提醒管理 | — | 打开 reminder_panel | ConfigFile |
| 踏板外观 | `platform_style` | 打开 platform_style_panel | ConfigFile |
| 待办清单 | — | 打开 todo_panel | ConfigFile |
| 运行功耗 | `auto_activity` | 三态 (待机/节能/性能) | ConfigFile |
| 随系统唤醒 | — | `HKCU\Run` 注册表。已迁移至装置终端「终端配置」Tab | 注册表 |
| 训练数据 | — | 已迁移至装置终端面板能力数据 Tab，含等级控制 + 互动按钮 | 只读+操作 |
| 外观主题 | — | 已迁移至装置终端「外观主题」Tab | ConfigFile |
| 机体诊断 | — | 已迁移至装置终端「机体诊断」Tab。从右键菜单系统信息迁移 | 只读 |
| 装置终端 | — | 独立面板 (layer=-1): 8 Tab (战绩/能力/提醒/日志/外观主题/机体诊断/关于/配置) | 只读+操作 |

---

## 九、性能优化策略

| 优化项 | 机制 |
|--------|------|
| DWM 穿透限流 | 完美模式仅在鼠标进出宠物区域时切换 `SetWindowLong` (1次/跨越)，零 GDI 开销 |
| 帧率锁定 | `max_fps=120` + `physics_ticks=120` 统一渲染和物理帧率 |
| 按需重绘 | 仅运动/特效/眼球动画/形变振荡时 `queue_redraw()`，静止时零绘制开销 |
| 进程提权 | `SetPriorityClass(ABOVE_NORMAL)` 对抗游戏等 CPU 抢占 |
| 系统信息异步 | 全部 PowerShell/WMI 查询通过 `WorkerThreadPool` 后台执行，零主线程卡顿 |
| 窗口隐身启动 | `visible=false` → 设置 → 等待 → `visible=true` → `HideFromTaskbar`，防任务栏闪烁 |
| 任务栏守护 | 每 5 秒自检 `EnsureHiddenFromTaskbar()`，防引擎焦点变化重置样式 |
| 碰撞体对象池 | 幽灵墙和虚空填充使用预分配对象池，避免每帧创建/销毁 |
| 本地气泡堆叠 | 每宠物独立气泡栈 (最多 3 条)，通过 `get_bubble_rects()` 注册 DWM 矩形 |
| 方向感知定位 | `_menu_side` 全局方向状态驱动所有面板弹出方向，屏幕任意位置方向正确 |

---

## 十、游戏扩展包系统 [已冻结]

> **代码冻结声明**: `games/` 目录下的全部代码 (含 `base_game.gd`、`game_chrome.gd`、`holo_compositor.gd` 及各游戏子目录) 以及 `core/game_manager.gd` 已停止维护。所有游戏相关新功能在 `ui/game_terminal/` 中开发。此处文档保留作为架构参考，旧代码可只读查阅用于视觉/逻辑借鉴。

### 设计思路

游戏作为**可选扩展包**，不内嵌在宠物本体中。开发时在同一项目 `games/` 目录编写（可直接调试），发布时每个游戏单独导出为 `.pck` 资源包，用户按需下载。

### 核心组件

| 组件 | 文件 | 职责 |
|------|------|------|
| BaseGame 基类 | `games/base_game.gd` | 定义 `start()` / `cleanup()` 接口 + `game_finished` 信号，管理面板挂载/定位/显隐、战绩/XP、自玩基础设施、教程面板。悬浮 UI 委托 GameChrome |
| GameChrome | `games/game_chrome.gd` | 悬浮组件系统 (RefCounted)。管理标题气泡/侧边按钮/连接线/发言气泡/重开按钮的创建、定位、动画、拖拽、清理。委托 HoloCompositor 管理全息合成视口 |
| HoloCompositor | `games/holo_compositor.gd` | 全息合成器。创建独立 SubViewport，将游戏面板纹理 + 悬浮组件克隆体合成为完整画面，供 PetHoloScreen 获取合成纹理 |
| PetHoloScreen | `entities/pet/pet_holo_screen.gd` | 全息屏渲染控制器。管理梯形透视渲染、投影支架线、屏幕矩形计算 |
| GameManager | `core/game_manager.gd` | 扫描游戏目录、创建 SubViewportContainer+SubViewport 包装层、管理启动/清理生命周期 |
| 菜单集成 | `ui/context_menu.gd` | 玩法分区 → 小游戏列表（菜单打开时动态构建） |
| EventBus 信号 | `launch_game(game_id)` | 菜单 → main.gd → GameManager 启动指定游戏 |

### BaseGame 接口契约

```gdscript
class_name BaseGame extends RefCounted

enum Result { WIN, LOSE, DRAW }
signal game_finished(result: Result)

# GameManager 注入的运行时引用 (start 前自动设置)
var game_viewport: SubViewport            # 游戏 UI 渲染到的 SubViewport
var game_container: SubViewportContainer   # 屏幕上的容器 (定位/拖拽用)
var screen_size: Vector2                   # 屏幕实际大小
var _pet: Node2D                           # 宠物原体引用

func get_game_id() -> String      # 唯一标识
func get_game_name() -> String    # 显示名称
func get_game_desc() -> String    # 简短描述
func start() -> void              # 启动 (构建 UI + 调用 _setup_floating_chrome)
func cleanup() -> void            # 清理 (悬浮组件/教程面板/全息视口)
func sync_viewport_size() -> void # 同步 SubViewport 大小到面板
func get_holo_texture() -> Texture2D # 获取全息合成纹理
```

### 全息屏合成投影

游戏 UI 采用解构式布局（面板 + 悬浮组件分离），BaseGame 委托 HoloCompositor 创建独立的全息合成视口，将游戏面板纹理 + 悬浮组件克隆体合成为完整画面。PetHoloScreen 通过 `get_holo_texture()` 获取合成纹理，梯形透视渲染到宠物侧面。

```
GameManager (CanvasLayer, layer=110)
  ├─ SubViewportContainer (定位/拖拽/动画)
  │    └─ SubViewport (transparent_bg)      ← 游戏面板本体
  ├─ 标题气泡 / 侧边按钮 / 重开按钮        ← 悬浮在面板外部
  └─ HoloCompositor 管理的合成视口:
       └─ SubViewport (全息合成, gui_disable_input)
            ├─ TextureRect (game_viewport 纹理)
            └─ 悬浮组件克隆体 (duplicate(0))

pet.gd _draw():
  └─ PetHoloScreen.render()
     └─ draw_polygon(梯形顶点, UV, 合成视口.get_texture())
        实时镜像完整 UI + 梯形透视 (近端收缩 15%)
```

### 游戏元数据格式 (`meta.json`)

```json
{
  "id": "tic_tac_toe",
  "name": "策略矩阵",
  "desc": "3x3 决策推演。本机不会输。",
  "version": "1.0.0",
  "script": "res://games/tic_tac_toe/game.gd"
}
```

### 已实现游戏

| 游戏 | ID | 渲染方式 |
|------|----|---------|
| 策略矩阵 (井字棋) | `tic_tac_toe` | Canvas `_draw()` 自绘 |
| 威胁评估 (扫雷) | `minesweeper` | Control 节点树 |
| 矩阵叠加 (2048) | `2048` | Control 手动布局 |
| 路径规划 (贪吃蛇) | `snake` | `BaseGame.GridRenderer` 自绘 |
| 结构堆叠 (俄罗斯方块) | `tetris` | `BaseGame.GridRenderer` 自绘 |

多种渲染方式均已验证 SubViewport 全息屏通用性。具体规则、话术、视觉设计等详见 [06_游戏扩展系统.md](06_游戏扩展系统.md)。

### 宠物游戏态

游戏启动时宠物进入专属游戏态，全部逻辑委托给 `PetGaming` (RefCounted)，通过 `pet_gaming_changed` 信号驱动：

| 行为 | 实现 |
|------|------|
| 锁定不动 | 高阻尼 (`linear_damp=20`) + 每帧清零速度，保留重力自然落地 |
| 状态保护 | `transition_to()` 中阻止非 idle/fall 的状态跳转，`idle.process()` 中跳过所有转换 |
| 瞳孔注视 | pet.gd 决议层: `gaming.active` 时强制 `forced_look_dir` 指向全息屏方向 (最高优先级) |
| 全息迷你屏 | PetHoloScreen 渲染: 固定尺寸 (2.0R×2.5R), HoloCompositor 合成视口纹理梯形透视渲染 (近端收缩 15%), 理想位置布局不受面板拖拽影响 |
| 微行为屏蔽 | 游戏态跳过所有微行为 (深夜休眠/白天待机/自检)，保留深夜时钟检测 |
| 交互拦截 | 拖拽被屏蔽 (idle/fall 状态)，点击宠物弹专属回应 4 条话术 |
| 空间跳跃保护 | 启动时取消正在进行的 roam，退出时设置 60 秒冷却 |
| 游戏结束 | 切 fall 状态自然过渡 (fall.enter 自动恢复阻尼) |
| 关闭流程 | 通过 `EventBus.close_game_requested` 信号解耦，GameManager 监听并清理 |
| 头顶关闭按钮 | 宠物头顶悬浮"自娱模式"标签，鼠标靠近切换为"中断游戏"(红色警告)。点击发射 `close_game_requested`，经 DWM 穿透注册可点击。与终端模式关闭按钮同风格 |

### 个人终端 (PetHoloScreen 多模式)

`PetHoloScreen` 不仅服务于游戏模式，还支持多种独立显示模式，让全息屏成为宠物的“个人电脑”：

| 模式 | 枚举值 | 说明 |
|------|--------|------|
| OFF | 0 | 全息屏关闭 |
| GAME | 1 | 游戏模式，HoloCompositor 提供纹理 |
| IDLE | 2 | 待机屏保 (透视网格/神经数据流/雷达准星/心电波形/边框护甲，全 UV 透视映射) |
| LOADING | 3 | 加载模式 (透视旋转弧线 + 脉冲环) |
| BATTERY | 4 | 电源模式 (全息动态电源监控，横向电池与分块充能指示灯) |
| DONE | 5 | 确认模式 (状态核查完成全息对勾动画、光晕散射) |
| MAIL | 6 | 邮件提醒模式 (全息信封/呼吸底纹/滚动数据流/红点提醒) |
| ERROR | 7 | 警报阻断模式 (深红警报闪烁、毛刺偏移乱码、警告断裂条纹) |
| WARNING | 8 | 系统警告模式 (琥珀色极简层叠细线三角、机械离散步进动态效果) |
| QUERY | 9 | 未知检索模式 (直接采纳系用户UI色，通过区块叠加闪烁构成的问号、扫描探测网以及悬浮的寻址数据碎片) |
| ALARM | 10 | 日程闹钟模式 (物理机械铃铛的高速震动、发散双侧脉冲声波、周围时钟走针光点) |
| CLEANUP | 11 | 碎片清理模式 (模拟粉碎机制，文件文档经过切割口裂解为垂直数据条落入经典垃圾桶槽内并清理) |
| GLOBE | 12 | 网络巡航雷达 (彩蛋：纯数学折叠 2D 的完整 3D 自转地球、带有信标脉冲侦测与立体光轨卫星网络) |
| SYNC | 13 | 常规网络通讯模式 (放大加粗的递进式高科感扇形波形、向外跃迁的握手交互粒子、以及全息测算闪烁的底部位符) |
| LOCK | 14 | 设备挂机锁定模式 (表现为系统闭门锁死的下落锁扣、底层稳固不跳动的验证占位块及保护栅栏网) |

**共享基础设施 (所有终端模式通用):**

| 组件 | 实现 |
|------|------|
| 踏板升起 | 复用 `FreeRoamSystem.PlatformVisual`，升起 15px 避免任务栏遮挡 |
| 宠物锁定 | 高阻尼 + 零重力 + idle 状态保护，点击宠物给专属话术 |
| 关闭按钮 | 宠物头顶悬浮“断开连接”按钮，经 DWM 穿透注册可点击 |
| 双态标签 | LOADING 模式默认显示状态文字，悬停宠物时切换为“断开连接” |
| 透视映射 | `_map_uv()` 双线性插值，将 UV 坐标映射到矩形/后仰顶点。透视(梯形)已移除，仅保留后仰 |
| 中间缩放 | `_mini_vp` SubViewport (240px) GPU 预缩小源纹理，避免 draw_polygon 极端下采样模糊 |
| 用户配置 | `holo_size` (1x~2x) 和 `holo_tilt` (0~25%) 通过 SettingsManager 持久化，装置终端「终端配置」Tab 滑条实时调节，支持预览 |
| 动态间距 | 游戏模式贴近 (`GAP_GAME=0.15`)、终端模式拉开 (`GAP_TERMINAL=0.5`)，切换时 `lerpf` 平滑过渡，有绝对上下限约束 |
| 模块化渲染 | 每个显示模式的视觉渲染抽离到 `holo_modes/` 独立文件，懒加载实例化，新增模式只需添加一个文件 |
| 终端模式判定 | `is_terminal_mode` 计算属性 (非 OFF 非 GAME 即为终端)，统一控制注视/间距/状态锁定/点击拦截，新增模式无需修改 |
| 注册表驱动 | `_MODE_REGISTRY` 字典集中管理所有终端模式的类名和标签，`_show_terminal()` 统一启动逻辑，新增模式只需: 1) enum 加值 2) 注册表加一行 3) 写渲染器文件 |
| 模式抢占 | `show_game()` 自动清理当前终端模式的资源 |

触发方式: `IdleActivities` 自动调度 (权重 33%) + 宠物分区「个人终端」子菜单手动触发。

### PCK 打包流程 (后续)

```
开发时:  games/tic_tac_toe/ → 同项目直接调试
发布时:  games/tic_tac_toe/ → 单独导出为 tic_tac_toe.pck
         宠物本体导出时排除 games/ 目录
用户侧:  下载 .pck → 放入 user://games/ → 宠物自动识别
```

---

## 十一、待办事项系统

待办面板、主动提醒、主题系统的完整技术文档已迁移至独立文档：

**→ [`docs/08_待办事项系统.md`](08_待办事项系统.md)**

要点：
- 基类 `TodoThemeBase` 提供种子色推算 + 中性默认工厂
- 主题子类自包含边框类 + 组件覆写，各自独立不干扰
- 新增骨架组件只需在基类加中性默认，已有主题零改动
- `_theme_template.gd` 提供新建主题的完整参考

---

## 开发注意事项

### 瞳孔方向 (forced_look_dir) 管理规范

`forced_look_dir` 是控制宠物瞳孔注视方向的核心变量。为防止多系统争抢冲突，采用「集中决议」模式：

**规则: 绝对禁止在 pet.gd 以外的任何文件中直接赋值 `eye_behavior.forced_look_dir`。**

所有来源均通过对应接口声明方向，pet.gd 每帧按优先级裁决:

```
gaming.active        → 游戏态方向 (最高)     ← 自动，无需干预
holo_screen 终端模式 → 全息屏方向            ← 自动，无需干预
movement.is_active   → movement.direction    ← 通过 PetMovement API
无活跃来源         → Vector2.ZERO (自然追踪)
```

#### 移动状态中设方向

```gdscript
# 需要先看方向再行动 (状态机 enter 中):
pet.movement.start(Vector2(hop_dir, 0), _execute_hop.bind(hop_dir))
# 缓冲期跳过物理:
if pet.movement.in_look_ahead: return
# exit 中结束:
pet.movement.finish()  # → HOLD 0.3s → IDLE
```

#### 已有自己节奏的移动 (free_roam 等)

```gdscript
# 跳过缓冲，直接激活:
pet.movement.activate(Vector2(hop_dir, 0))
# 落稳后:
pet.movement.finish()
```

#### 打断移动 (拖拽/休眠唤醒)

```gdscript
pet.movement.cancel()  # 立即清零方向
```

### 反重力 (anti_gravity) 视觉适配规范

反重力时宠物视觉翻转 180°。为避免每个功能都单独适配，`pet.gd` 提供两个集中计算属性：

| 属性 | 含义 | 值 |
|------|------|------|
| `visual_rotation` | 视觉旋转角度 | `rotation + PI`(反重力) / `rotation`(正常) |
| `ag_flip` | 方向转换因子 | `-1.0`(反重力) / `1.0`(正常) |

**规则: 视觉渲染代码不应直接检查 `anti_gravity`，而是使用这两个属性。**

#### 何时用 `visual_rotation`

渲染需要跟随宠物翻转的内容时 (宠物本体、全息屏等)：

```gdscript
# 构建绘图变换 (替代直接用 rotation):
var r_mat = Transform2D(pet.visual_rotation, Vector2.ZERO)

# 反旋转到世界坐标系:
pet.draw_set_transform(Vector2.ZERO, -pet.visual_rotation, Vector2.ONE)
```

#### 何时用 `ag_flip`

将世界空间方向转为视觉空间方向时 (瞳孔偏移、全息屏侧向等)：

```gdscript
# 瞳孔偏移补偿:
var iris_offset = eye_behavior.get_pupil_offset() * ag_flip

# 全息屏视觉侧向:
var visual_side = holo_screen.side * ag_flip
```

#### 保留 `anti_gravity` 直接检查的场景

- **物理层**: 踏板碰撞体旋转、重力方向等 (用 `gravity_sign`)
- **UI 定位层**: 气泡、按钮、HUD 的上下方位置计算 (用户看的 UI 不用翻转)

---

## 开发避坑

### CanvasLayer 低层级 UI 的 mouse_filter 穿透问题

当 UI 面板使用低于宠物的 CanvasLayer (如 `layer = -1`，使宠物渲染在面板之上) 时，面板上的 Control 节点如果是 `MOUSE_FILTER_STOP` (默认值)，会在 GUI 输入阶段吞掉鼠标事件，导致宠物的 `_unhandled_input` 收不到点击/拖拽事件。

**关键认知**: GUI 输入跨 CanvasLayer 处理，先于 `_unhandled_input`。低层级的 Control STOP 会阻断高层级场景节点的输入。

| 控件类型 | mouse_filter 策略 | 说明 |
|---------|------------------|------|
| 结构性容器 (PanelContainer / MarginContainer / VBox / HBox / ScrollContainer) | `MOUSE_FILTER_PASS` | 让事件继续传播到 `_unhandled_input` |
| 纯装饰元素 (Label / Separator / 占位 Control) | `MOUSE_FILTER_IGNORE` | 完全不参与鼠标事件处理。**特别注意: 空 Control 做 spacer 时默认 STOP，极易遗漏** |
| 交互按钮 (需要在宠物之下可点击) | `MOUSE_FILTER_PASS` + `gui_input` 手动处理 | 在回调中检查 `pet.is_mouse_on_pet()`，宠物在上方时 return 让事件穿透到宠物 |

```gdscript
# 标准做法: 宠物优先的按钮点击
btn.mouse_filter = Control.MOUSE_FILTER_PASS
btn.gui_input.connect(func(event: InputEvent):
    if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT and event.pressed:
        var pet = _get_pet()
        if pet and pet.is_mouse_on_pet():
            return  # 宠物优先，不处理
        _on_btn_action()
)
```

### DWM 穿透策略选择: `context_menu_toggled` vs `overlay_rects`

新增独立面板时必须选择正确的穿透策略，选错会导致面板不可点击或阻塞桌面其他程序：

| 策略 | 信号/机制 | 效果 | 适用场景 |
|------|----------|------|----------|
| 全窗口阻塞 | `context_menu_toggled.emit(true/false)` | 整个 Godot 窗口变为不穿透，桌面其他程序收不到鼠标事件 | 右键菜单、待办面板、踏板外观等小型弹窗 |
| 精确区域 | `pet.set_overlay_rect(key, Rect2(...))` (每帧更新) | 仅面板区域不穿透，面板外的区域继续穿透到桌面 | 装置终端、游戏终端等大面板 |

注意事项：
- `overlay_rects` 是 Dictionary (key→Rect2)，多面板可同时注册各自的命中区域，互不干扰
- 面板关闭时必须调用 `pet.remove_overlay_rect(key)` 注销自己的区域
- 使用精确区域策略的面板支持点击置顶 (`panel_focus_requested` 信号)，重叠区域仅顶层面板响应
- 不要混用两种策略，否则全窗口阻塞会覆盖精确区域的效果

